{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}

        <div class="card text-left my-3">
            <div class="card-header">
                User id: {{ user.id }}
            </div>
            <div class="card-body">
                <p class="card-text">First name : {{ user.surname }}</p>
                <p class="card-text">Last name : {{ user.name }}</p>
                <p class="card-text">Email : {{ user.email }}</p>
                <p class="card-text">Role : {{ user.roles ? user.roles|json_encode : '' }}</p>
            </div>
        </div>
        <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn btn-primary">Edit my informations</a>
        <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn btn-danger">Delete account</a>



    {# <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a> #}

    {# {{ include('user/_delete_form.html.twig') }} #}

{% endblock %}